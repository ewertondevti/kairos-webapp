# Settings for Backend (on Cloud Run).
# See https://firebase.google.com/docs/app-hosting/configure#cloud-run
runConfig:
  minInstances: 0
  # maxInstances: 100
  # concurrency: 80
  # cpu: 1
  # memoryMiB: 512

# Environment variables and secrets.
env:
  - variable: NEXT_PUBLIC_PROJECT_ID
    value: kairos-webapp-4de65
    availability:
      - BUILD
      - RUNTIME
  - variable: NEXT_PUBLIC_DEFAULT_USER_TOKEN
    secret: NEXT_PUBLIC_DEFAULT_USER_TOKEN
    availability:
      - BUILD
      - RUNTIME
