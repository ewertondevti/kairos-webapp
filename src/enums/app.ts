export enum LocalKey {
  Token = "token",
}

export enum DatabaseTableKeys {
  Images = "images",
  Albums = "albums",
  Events = "events",
  Members = "members",
  Users = "users",
  AccessRequests = "accessRequests",
}
